defaults:
  - ms_hard
  - /sampler@hard.samplers.sim_adaptive: sim_adaptive

hard:
  # Override batch size to be smaller
  num_samples: 2  # Original was 500
  save_samples: true
  sampling_video: true
  samplers:
    sim_adaptive:
      max_steps: 300
      top_k: 1