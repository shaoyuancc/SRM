defaults:
  - ms_hard
  - /sampler@hard.samplers.sim_weighted_adaptive: sim_weighted_adaptive

hard:
  # Override batch size to be smaller
  num_samples: 2  # Original was 500
  save_samples: true
  sampling_video: true
  samplers:
    sim_weighted_adaptive:
      max_steps: 300
      uncertainty_power: 10
